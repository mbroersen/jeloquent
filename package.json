{
  "name": "jeloquent",
  "module": "dist/jeloquent.js",
  "unpkg": "dist/jeloquent.js",
  "main": "dist/jeloquent.js",
  "version": "3.0.2",
  "description": "micro framework / memory orm model store",
  "scripts": {
    "build": "./node_modules/.bin/esbuild ./src/Jeloquent.js --bundle --format=esm --platform=browser --minify --keep-names --outfile=./dist/jeloquent.js",
    "watch": "./node_modules/.bin/esbuild ./src/Jeloquent.js --bundle --format=esm --platform=browser --minify --keep-names --outfile=./dist/jeloquent.js --watch",
    "test": "jest",
    "lint": "eslint src/*/** --fix-dry-run"
  },
  "keywords": [
    "eloquent",
    "framework",
    "models",
    "orm",
    "datastorage",
    "memory",
    "store",
    "laravel",
    "data"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mbroersen/jeloquent.git"
  },
  "bugs": {
    "url": "https://github.com/mbroersen/jeloquent/issues"
  },
  "funding": {
    "type": "patreon",
    "url": "https://www.patreon.com/mbroersen"
  },
  "homepage": "https://github.com/mbroersen/jeloquent#readme",
  "author": "Mark Broersen",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.18.0",
    "jest": "^26.6.3",
    "@babel/core": "^7.16.5",
    "@babel/preset-env": "^7.16.5",
    "babel-jest": "^27.4.5"
  },
  "dependencies": {
    "esbuild": "^0.14.8",
    "typescript": "^4.2.4"
  }
}
